# Отчет по практическим работам №1-4 (Telegram-бот «TravelBuddy»)

**Студент:** Гисина Анастасия  
**Группа:** ОБ-АД-2023  
**Дата сдачи:** 2025-10-15  

---

## 1. Практическая работа №1 - методические рекомендации и план

### 1.1 Цель и ожидаемые результаты

Цель: спроектировать и реализовать Telegram-бота **TravelBuddy** для планирования поездок с учетом работы с внешними API, нестабильного сетевого соединения и минимального хранения пользовательского контекста.

Ожидаемые результаты:

- сформирована архитектура бота (Telegram + набор REST-сервисов);
- определены требования к окружению и конфигурации (`.env`, API-ключи);
- описаны риски (лимиты API, ошибки сети, потеря контекста) и меры обработки;
- подготовлена схема взаимодействия (файл `schema.png/schema.drawio`).

### 1.2 План-график (пример)

- **День 1** - анализ постановки задачи:
  - какие сценарии нужны пользователю (страна, новости, курс валют, погода, достопримечательности, праздники, маршрут);
  - какие данные нужны и из каких источников.
- **День 2** - выбор архитектуры:
  - Telegram-бот на `pyTelegramBotAPI`;
  - интеграция с REST-сервисами (RestCountries, WorldNewsAPI, apilayer, Open-Meteo, Wikipedia, Nager.Date).
- **День 3** - протоколы обмена и конфигурация:
  - HTTP(S)-запросы к внешним API;
  - настройка `.env`, логирования, обработка таймаутов.
- **День 4** - проработка устойчивости:
  - обработка ошибок/исключений;
  - валидация входных данных (город, страна, CSV);
  - деградация функционала при недоступности отдельных сервисов.
- **День 5** - описание и презентация:
  - итоговая схема взаимодействия;
  - README с инструкциями по запуску и ограничениями.

### 1.3 Дневник (вкратце)

- **Неделя 1:** сформулированы основные сценарии TravelBuddy: выбор страны, новости, курсы валют, погода, достопримечательности, праздники, импорт маршрута из CSV. Изучены требования к API (формат запросов/ответов, ограничения по ключам и лимитам).
- **Неделя 2:** выбран стек: Python 3.10+, библиотека `pyTelegramBotAPI`, хранение минимального контекста в памяти процесса (`USER_CTX`), переменные окружения через `python-dotenv`. Определены используемые API: RestCountries, WorldNewsAPI, Open-Meteo, apilayer `currency_data`, Wikipedia GeoSearch, Nager.Date.
- **Неделя 3:** проработана обработка сетевых ошибок (таймауты, коды ответа, пустые ответы), базовое логирование в stdout, форматирование ответов для Telegram (HTML-разметка, эмодзи, клавиатура).

### 1.4 Индивидуальное задание (резюме)

Индивидуальное задание - спроектировать бота, который:

- по выбранной стране подтягивает:
  - столицу, валюту и координаты (RestCountries);
  - новости за последнюю неделю (WorldNewsAPI);
  - кросс-курс валюты пользователя к валюте страны (apilayer `currency_data`);
  - погоду по столице/городу (Open-Meteo);
  - ближайшие достопримечательности в радиусе 5 км (Wikipedia GeoSearch);
  - ближайшие государственные праздники (Nager.Date);
- умеет принимать CSV с маршрутными точками и строить черновой план по дням.

Архитектура: один Telegram-бот с минимальным локальным контекстом (страна, валюта, координаты, базовая валюта пользователя), вся остальная логика - через запросы к внешним API. Для устойчивости предусмотрена обработка исключений и «мягкое» уведомление пользователя при ошибках источников.

### 1.5 Аналитический раздел

Основные риски:

- **Сетевые риски:** недоступность отдельных API (HTTP 4xx/5xx), таймауты, превышение лимитов.
- **Надежность данных:** частичная или неточная информация (например, отсутствие города в геокодере, отсутствие новостей за период).
- **Состояние пользователя:** контекст хранится только в оперативной памяти и теряется при перезапуске бота.
- **Многосервисность:** бот зависит сразу от нескольких внешних сервисов; отказ любого из них не должен «ронять» все приложение.

Принятые меры:

- таймауты в запросах (`timeout=10–12`);
- проверка `status_code` и структуры JSON-ответов;
- отлавливание исключений (`try/except`) и возврат пользователю понятного сообщения:

  - «Не удалось получить погоду: …»
  - «Не удалось выбрать страну: …»
  - «Не удалось загрузить новости: …» и т.п.

Также в ответах есть дисклеймер:

> «ℹ️ Данные из публичных API. Возможны задержки/неточности.»

### 1.6 Итоги и предложения

По итогам практической работы №1:

- сформирована архитектура TravelBuddy и список используемых API;
- описаны ограничения по контексту (in-memory) и устойчивости к ошибкам.

Возможные улучшения:

- вынести контекст пользователей в постоянное хранилище (БД) для сохранения выбранных стран между перезапусками;
- добавить кэширование часто запрашиваемых данных (например, праздники по стране) на короткий срок;
- реализовать ограничение частоты запросов к API для отдельного пользователя (rate-limiting).

---

## 2. Практика №2 - первый запуск Telegram-бота

**Сделано:**

- Реализован запуск бота в режиме long polling:

  ```bash
  python bot.py
  ```

  при наличии `BOT_TOKEN` в `.env`.

- Добавлены базовые команды:

  - `/start` - приветствие, описание роли TravelBuddy и вывод главного меню;
  - `/help` - справка по командам и краткое описание сценариев;
  - меню-клавиатура с быстрыми кнопками:
    - «🎯 Выбрать страну»
    - «🌍 Что выбрано»
    - «📰 Новости»
    - «💱 Курс валют»
    - «🌦 Погода»
    - «🗺️ Достопримечательности»
    - «🎉 Праздники»
    - «🆘 Помощь».

- Реализована CLI-демонстрация без Telegram:

  ```bash
  python bot.py --country Japan
  ```

  В этом режиме бот выводит краткую сводку по выбранной стране (название, код, столица, валюта, координаты).

**Скриншоты:**  
(вставить скриншоты /start, /help, главного меню Telegram-бота)

---

## 3. Практическая работа №3 - обработка текстов и внешние API

### 3.1 Обработка текстовых сообщений и команд

Реализовано:

- Команды:

  - `/set_destination <страна>` - выбор страны назначения (на английском, через RestCountries API).
  - `/country` - краткий обзор текущей выбранной страны (название, столица, валюта, координаты).
  - `/news` - новости за последнюю неделю по выбранной стране.
  - `/fx [base=EUR]` - кросс-курс базовой валюты пользователя к валюте выбранной страны (через USD).
  - `/weather [город]` - погода по столице или указанному городу.
  - `/sights [город]` - достопримечательности в радиусе ~5 км (Wikipedia GeoSearch).
  - `/holidays` - ближайшие государственные праздники для выбранной страны.
  - `/csv_template` - выдача CSV-шаблона для маршрута.

- Ключевые фразы в обычном тексте, которые бот распознает и перенаправляет на нужную команду:

  - «страна», «поездк…» → подсказка по `/set_destination`;
  - «погод…», «weather» → вызов логики `/weather`;
  - «новост…», «news» → вызов `/news`;
  - «валют…», «курс», «fx» → вызов `/fx`;
  - «места», «достопр…», «sights» → вызов `/sights`;
  - «праздн…», «holiday» → вызов `/holidays`.

### 3.2 Внешние API и формат данных

Используемые API и их роль:

- **RestCountries** - получение базовой информации по стране: название, код, столица, координаты, валюты.
- **WorldNewsAPI** - новости за последнюю неделю по стране (фильтрация по стране и языку, приоритет ru → fallback на любой язык).
- **apilayer `currency_data`** - живые курсы валют к USD, расчет кросс-курсов базовая валюта ↔ валюта страны.
- **Open-Meteo**:
  - геокодер (поиск города и координат);
  - погодный API (текущая температура, ветер, влажность, код погоды, часовой пояс).
- **Wikipedia GeoSearch** - поиск точек интереса по координатам, затем запрос деталей (описание, ссылку, миниатюру) через отдельный запрос.
- **Nager.Date** - ближайшие государственные праздники по ISO-коду страны.

Данные в основном приходят в формате **JSON**. В Python используется:

```python
resp = requests.get(url, params=params, timeout=..., headers={...})
data = resp.json()
```

Дальше данные преобразуются в словари/списки и форматируются для красивого HTML-ответа в Telegram.

### 3.3 Обработка ошибок и защита от «плохих» ответов

Во всех основных функциях предусмотрены:

- таймауты запросов (`timeout=10–12`);
- проверки `status_code` и содержимого JSON (наличие полей, тип данных);
- отлавливание исключений (`try/except`) и возврат пользователю понятного сообщения:

  - «Не удалось получить погоду: …»
  - «Не удалось выбрать страну: …»
  - «Не удалось загрузить новости: …» и т.п.

Также в ответах есть дисклеймер:

> «ℹ️ Данные из публичных API. Возможны задержки/неточности.»

### 3.4 Контрольные вопросы (краткие ответы)

1. **Что такое API в контексте TravelBuddy?**  
   Это HTTP-интерфейс, через который бот получает данные о странах, погоде, новостях, курсах валют, достопримечательностях и праздниках в формате JSON.

2. **Как происходит обработка JSON-ответа?**  
   Сначала вызывается `resp.json()`, затем бот выбирает нужные поля (например, `news`, `quotes`, `current`, `query.geosearch`) и формирует человекочитаемое сообщение для Telegram.

3. **Какие типовые ошибки учитываются?**  
   Таймауты сети, коды 4xx/5xx, пустые списки (нет новостей, нет достопримечательностей), отсутствие нужных полей, неверные параметры (неизвестная страна/город). Все ошибки оборачиваются в `try/except` и пользователю выдается мягкое объяснение.

4. **Зачем нужен дисклеймер?**  
   Чтобы честно указать, что данные берутся из внешних публичных API, могут быть неточными или неполными, и бот - учебный помощник, а не «официальный источник» прогноза погоды или новостей.

---

## 4. Практическая работа №4 - работа с CSV и мини-отчет по маршруту

Вместо финансового отчета реализован сценарий, соответствующий тематике TravelBuddy: **генерация чернового плана поездки из CSV-файла с точками маршрута**.

### 4.1 Реализовано

- Команда `/csv_template` отправляет пользователю CSV-шаблон с колонками:

  ```text
  name,lat,lon,duration_min,priority
  ```

- При загрузке пользователем CSV-файла:

  - бот считывает файл в `pandas.DataFrame`;
  - нормализует данные (`_normalize_csv`):
    - принимает синонимы колонок (например, `title`, `place` → `name`, `latitude` → `lat`, `longitude` → `lon`);
    - приводит числовые поля к типу `float`/`int`;
    - по необходимости добавляет колонку `priority` (дефолт 3, если не задана).
  - строит маршрут:

    - сортировка точек по приоритету (от более важных к менее важным);
    - эвристика «ближайшего соседа» с учетом расстояния от текущей локации (координаты столицы или первой точки);
    - разбиение на дни по дневному бюджету времени (по умолчанию ~420 минут в день), учитывая:
      - время перехода между точками (расстояние в км × мин/км);
      - время посещения точки (`duration_min`).

- Итоговый план форматируется функцией `fmt_itinerary`:

  - выводится заголовок с выбранной страной/столицей (если они заданы);
  - для каждого дня - список точек с примерным временем перехода и посещения;
  - суммарное время ходьбы и посещения за день;
  - дисклеймер о приблизительности расчетов.

### 4.2 Примеры запуска

- **Получение шаблона:**

  В Telegram: `/csv_template` → бот присылает файл `travel_template.csv` с примером.

- **Загрузка собственного маршрута:**

  Пользователь отправляет CSV-файл с колонками `name, lat, lon, duration_min (priority)` в чат → бот отвечает текстовым планом по дням.

- **CLI-режим (демо):**

  ```bash
  python bot.py --country Japan
  ```

  выводит краткий отчет по выбранной стране (без Telegram).

### 4.3 Каркас мини-отчета о маршруте (пример вывода)

- Заголовок: «🗺️ План поездки - [страна / столица]»
- Настройки:
  - дневной бюджет времени;
  - скорость передвижения (мин/км).
- Для каждого дня:
  - нумерованный список точек (с указанием времени перехода и посещения);
  - суммарное время ходьбы и посещений за день.
- Итоговый дисклеймер о примерности планирования.

### 4.4 Контрольные вопросы (краткие ответы)

- **Чем отличается работа с локальным CSV и с API?**  
  CSV - локальный файл пользователя: данные детерминированные, но ответственность за корректность формата на пользователе. API - удаленный сервис: данные актуальные и живые, но могут быть недоступны, с лимитами и ошибками.

- **Какие метрики/показатели уместны для маршрута?**  
  Суммарное время посещения, суммарное время переходов, количество точек в день, распределение приоритетов. Эти показатели помогают пользователю понять нагрузку по дням.

- **Какие типичные ошибки источника приходится обрабатывать?**  
  Неверная кодировка или структура CSV, отсутствие обязательных колонок, некорректные координаты, нулевые/отрицательные длительности, пустой результат после фильтрации. В этих случаях бот выдает пользователю пояснение и совет по исправлению.

- **Зачем нужен дисклеймер в отчете по маршруту?**  
  Чтобы подчеркнуть, что расчеты времени и порядка - приблизительные, основаны на простой эвристике и публичных данных, и конечное решение по плану поездки остается за пользователем.

---

## Приложения

- `bot.py` - исходный код бот-логики TravelBuddy.
- `requirements.txt` - зависимости проекта.
- `.env.example` - пример файла окружения с переменными:
  - `BOT_TOKEN`
  - `WORLDNEWS_API_KEY`
  - `CURRENCY_API_KEY`
  - `WM_USER_AGENT`.
- `schema.png` / `schema.drawio` - схема взаимодействия бота с внешними сервисами.
- (опционально) директория `data/` для пользовательских CSV-маршрутов.
